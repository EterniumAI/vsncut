---
const { s } = Astro.props;
const paras = String(s.body ?? '').split(/\n{2,}/).filter(Boolean);
---
<section class="about" id="about">
  <div class="wrap">
    <h2>{s.heading}</h2>
    {paras.map((p: string) => <p class="sub">{p}</p>)}
  </div>
</section>
